#include "mbed.h"

#define WAIT_TIME_MS 1000
#define WAIT_TIME_MS2 4000

DigitalOut pin1(PA_5);

int main()
{
    while (true)
    {
        thread_sleep_for(WAIT_TIME_MS2);
        pin1 = !pin1;
        thread_sleep_for(WAIT_TIME_MS);
    }
}
